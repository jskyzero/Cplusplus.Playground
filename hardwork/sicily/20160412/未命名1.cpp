#include <iterator>
#include <set>
using namespace std;
set<int[10]> s;
set<int[10]>::iterator p;
for (p = s.begin(); p != s.end(); p++) {
	cout << (*p)[1];
}
set<pair<int, int>> t;
set<pair<int, int>>::iterator p1;
pair<int, int> tt;
tt.first, tt.second;
for (p1 = t.begin(); p1 != t.end(); p1++) {
	cout << (*p1).first;
}
